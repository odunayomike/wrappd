import * as React from 'react';
export interface CurationFormProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const CurationForm: React.FC<CurationFormProps>;
export default CurationForm;
