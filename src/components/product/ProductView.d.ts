import * as React from 'react';
export interface ProductViewProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const ProductView: React.FC<ProductViewProps>;
export default ProductView;
